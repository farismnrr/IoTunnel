{
	"name": "mqtt-client",
	"module": "server.ts",
	"type": "module",
	"scripts": {
		"start": "bun --watch src/server.ts",
		"build": "bun build src/server.ts --compile --minify --outfile dist/mqtt-server",
		"deploy": "pm2 start dist/mqtt-server"
	},
	"devDependencies": {
		"@types/bcrypt": "^5.0.2",
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	},
	"dependencies": {
		"@hapi/hapi": "^21.3.10",
		"auto-bind": "^5.0.1",
		"axios": "^1.7.7",
		"bcrypt": "^5.1.1",
		"node-fetch": "^3.3.2"
	}
}
